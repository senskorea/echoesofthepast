# Server-only; never use VITE_ for these secrets.
GEMINI_API_KEY=
OPENAI_API_KEY=
GENERATION_ENABLED=false
ALLOWED_ORIGINS=https://senskorea.github.io,http://localhost:8080,http://127.0.0.1:4174
# SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY are injected by Supabase.
# Also set enabled/global_daily/visitor_daily in generation_limits after agreeing
# limits. Zero defaults disable each action even if GENERATION_ENABLED is true.
